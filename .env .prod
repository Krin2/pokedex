# Este archivo especifica las variables de entorno de produccion que van a ser utilizadas

# Se podria colocar directamente la misma cadena de coneccion hacia el mongo atlas como esta en el .env,
# pero queremos que la coneccion la haga a la DB que tenemos en la imagen de nuestro docker.
# MONGODB=mongodb://<conteiner_name>:<puert expuesto>/<nombre de la collection en la base de datos>

MONGODB=mongodb://mongo-poke:27017/nest-pokedex

PORT=3000
DEFAULT_LIMIT: 5

